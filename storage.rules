rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /users/{userId}/{allPaths=**} {
      allow read: if true; // Publicly readable for story sharing/display
      allow write: if request.auth != null && request.auth.uid == userId;
    }
  }
}